import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - Light Pink Theme
  static const Color primary = Color(0xFF0f88ff); // Main pink color
  static const Color primaryDark = Color(0xFF0f88ff); // Darker pink
  static const Color primaryLight = Color(0xFF0f88ff); // Lighter pink
  
  // Secondary colors
  static const Color secondary = Color(0xFF424242);
  static const Color secondaryDark = Color(0xFF212121);
  static const Color secondaryLight = Color(0xFF616161);
  
  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);
  
  // Background colors
  static const Color background = Color(0xFFFFFFFF); // Pure white
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);
  
  // Text colors - Black and Grey theme
  static const Color textPrimary = Color(0xFF000000); // Pure black
  static const Color textSecondary = Color(0xFF666666); // Medium grey
  static const Color textHint = Color(0xFF999999); // Light grey
  
  // Border colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFE0E0E0);
}
